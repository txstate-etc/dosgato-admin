import type { UITemplate } from '@dosgato/templating'
import PageTemplate4Dialog from './PageTemplate4Dialog.svelte'

export const pageTemplate4: UITemplate = {
  templateKey: 'keyp4',
  dialog: PageTemplate4Dialog,
  pageBarButtons: [{ label: 'Styles' }]
}
